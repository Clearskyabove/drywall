extends ../../layouts/default

block head
  title Sign Up

block feet
  script(src='/views/signup/social.js')

block body

  script(type='text/javascript')
    var subdomainUsed = "#{subdomainUsed}";

  div.page-header
    h1 Complete Sign Up
  #signup
  
  script(type='text/template', id='tmpl-signup')
    form.form-horizontal
      div.alerts
        |<% _.each(errors, function(err) { %>
        div.alert.alert-error
          button.close(type='button', data-dismiss='alert') &times;
          |<%= err %>
        |<% }); %>
      div.control-group(class!='<%= errfor.email ? "error" : "" %>')
        label.control-label Enter Your Email:
        div.controls
          input(type='text', name='email', value!='<%= email %>')
          span.help-inline <%= errfor.email %>
      if subdomainUsed
        div.control-group(class!='<%= errfor.subdomain ? "error" : "" %>')
          label.control-label Claim Subdomain:
          div.controls
            input(type='text', name='subdomain', value!='<%= subdomain %>')
            span.help-inline <%= errfor.subdomain %>
      div.form-actions
        button.btn.btn-primary.btn-signup(type='button') Create My Account
  
  script(type='text/template', id='data-email') !{email}